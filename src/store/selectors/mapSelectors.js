export const selectMyLocation = (state) => state.map.myLocation;
export const selectOnlineUsers = (state) => state.map.onlineUsers;
export const selectCardChosenOption = (state) => state.map.cardChosenOption;
